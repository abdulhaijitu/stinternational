project_id = "tmdktmnoeqezkbdaaayh"

# Allow public (guest) checkout to call this function without requiring a JWT
[functions.create-order]
verify_jwt = false

# Allow public access to sitemap for SEO crawlers
[functions.generate-sitemap]
verify_jwt = false
